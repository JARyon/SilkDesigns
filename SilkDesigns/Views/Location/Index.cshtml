@using SilkDesign.Models;
@model IEnumerable<Location>


@{
    ViewData["Title"] = "Read Location";
}
<h1>Locations <a asp-action="Create" class="btn btn-sm btn-secondary">Create</a></h1>
<table class="table table-bordered table-sm table-striped">
    <thead>
        <tr>
            <th>Name</th>
            <th>Description</th>
            <th>Update</th>
        </tr>
    </thead>
    <tbody>
        @if (Model == null)
        {
            <tr><td colspan="5" class="text-center">No Model Data</td></tr>
        }
        else
        {
            @foreach (var p in Model)
            {
                <tr>

                    <td>@p.Name</td>
                    <td>@p.Description</td>
                    <td><a asp-action="Update" asp-route-id="@p.LocationID">Update</a></td>
                </tr>
            }
        }
    </tbody>
</table>