@using SilkDesign.Models;
@model IEnumerable<Route>


@{
    ViewData["Title"] = "Read Route";
}
<h1>Routes <a asp-action="Create" class="btn btn-sm btn-secondary">Create</a></h1>
<table class="table table-bordered table-sm table-striped">
    <thead>
        <tr>
            <th>Name</th>
            <th>Description</th>
            <th>Update</th>
        </tr>
    </thead>
    <tbody>
        @if (Model == null)
        {
            <tr><td colspan="5" class="text-center">No Model Data</td></tr>
        }
        else
        {
            @foreach (var p in Model)
            {
                <tr>

                    <td>@p.Name</td>
                    <td>@p.Description</td>
                    <td><a asp-action="Update" asp-route-id="@p.RouteId">Update</a></td>
                </tr>
            }
        }
    </tbody>
</table>